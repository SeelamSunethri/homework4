def encrypt(type):
    translated = ''
    i =len(type) - 1
    while i >= 0:
        translated = translated + type is :" , translated)
        return translated 

        def decrpyt(type):
            translated = ' 
            i = len(type) - 1
            while i>=0:
                translated = translated + type[i]
                i=i+1
                print("the decrpt type ",translated)
                message = "CLEASER CIPHER DEMO"
                x=encrypt(message)
                print(x)
                decrypt(x)
